- Prefers targeted but holistic polish on the imported/final design rather than a broad redesign: refine the requested section end-to-end so its components match the existing UI's aesthetic, spacing, hierarchy, affordances, dimensions, and especially light-mode color harmony; use one consistent border treatment/color across component variants, retaining an isolated accent only when it has a clear system-wide rationale rather than being decorative. Confidence: 0.95
- Prefers reusing established component color systems as explicit references instead of inventing new colors: match a card to the analogous card elsewhere in the product, including keeping the same card visually consistent across screens such as Beranda and Plan; allow deliberate theme-specific color mappings or swaps in dark mode when requested. Preserve each mode's foreground, divider, and contrast relationships along with the background. Confidence: 0.8
- Each screen/page in the app maintains its own distinct visual styling by default; cross-screen color unification is exploratory and easily reverted — the user tests a visual alignment, then decides to keep or roll back. Do not assume matching across screens is permanent; expect reversals. Confidence: 0.85
