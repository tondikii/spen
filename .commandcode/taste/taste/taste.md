# Taste
- Prefers a confirm-before-write workflow: agent explores the repo, presents a summary with recommended defaults, and waits for approval before writing any files or making system-level changes (e.g., installing tooling). Confidence: 0.7
- Communicates tersely — short approvals ("go", "langsung start", "sudah") and terse letter-based answers ("b", "a + d", "a") to structured interview questions; expects the agent to proceed without further questions. Confidence: 0.9
- Trusts recommended defaults for project tooling setup (e.g., GitHub Issues + gh CLI, default triage labels) rather than customizing options. Confidence: 0.7
- Prefers GitHub-native tooling for issue tracking when the repo is hosted on GitHub. Confidence: 0.6
- Approves installing required developer tooling (e.g., `gh` CLI via winget) when the need is explained and consent is asked first, rather than deferring the blocked step. Confidence: 0.5
- Buys into a structured skill-based workflow (grill → spec → tickets → per-ticket implement with TDD + code review) and defers to it for how work is sequenced; invokes the next phase directly without renegotiating the process. Confidence: 0.8
- Consistently picks the simpler, lower-configuration option when given product/design choices (e.g., monthly period with adjustable start date over fully custom periods, single global transfer category, one-shot over interactive AI, single daily transaction view over a two-mode/aggregated view, dropping a wallet "type" field and an onboarding period-setup step entirely) rather than flexible/complex alternatives, and justifies it by "cleaner" UX with less to maintain. Confidence: 0.95
- Delegates product/design decisions wholesale to the agent: when asked a batch of questions, usually accepts the recommended answer for every item ("Aku ikut semua rekomendasi mu") instead of deliberating per-option. Lead with a single recommended path rather than presenting open menus; the user explicitly asks for the agent's recommendation (e.g., "sarankan yang tepat promptnya"). Exception: on core structural/data-model questions the user will state an independent preference over the recommendation (e.g., chose a single global budget plan over the recommended per-wallet plan). Confidence: 0.97
- Communicates in Bahasa Indonesia and expects the agent to respond in the same language. Confidence: 0.9
- Prefers a design-first workflow: prototypes the UI with AI design tools (e.g., Stitch, then Figma Make) from a shared design brief before tickets are generated, so each ticket can reference visual design. Confidence: 0.7
- Evaluates AI design tools empirically — tries each one, judges the result ("cukup ok"), and moves on to the next candidate rather than committing to a single tool upfront (Stitch → Figma Make → Open Design). Confidence: 0.7
- Wants prompting guidance tailored to each specific tool's actual interface and capabilities (e.g., asked for a prompt refined specifically for Figma Make), not generic one-size-fits-all prompts. Confidence: 0.7
- Wants design artifacts kept in a dedicated repo directory (e.g., `docs/design/`) so they can be reused later, rather than left as chat-only content. Confidence: 0.7
- When a scope/decision changes, expects the change to be propagated to every related artifact — spec, glossary/CONTEXT, design brief, design prompts, and the published GitHub issue — so all documents stay consistent; will spot contradictions between docs (e.g., a user story implying a feature that an out-of-scope list excludes) and resolve them by expanding scope and updating everything related ("Ubah semua yang terkait dengan ini"). Also explicitly checks that every locked discussion decision actually lands in the artifacts ("pastikan semua hasil diskusi kita diterapkan ya") rather than staying chat-only. Confidence: 0.8
- Expects the agent to stay within the current discussion context and abstraction level: when discussing app flow / navigation & screen structure at the design level, do not jump ahead to source code or more technical documents; the user explicitly corrects such jumps ("aku belum nanyain sampe ke source code juga, lagi bahas alur aplikasi struktur Navigasi & Layar ini"). Confidence: 0.6
- Proactively proposes navigation/information-architecture decisions with an explicit rationale instead of waiting for options (e.g., Settings as a bottom tab rather than a header item because there is no profile data to fill the header; a central + button in the tab bar for the primary create action; wallet CRUD and the transaction list consolidated on Home with full lists reached via a "Lihat Semua" drill-down screen), then asks the agent to validate the idea ("Gimana kalo gitu oke ga?"). Confidence: 0.7
- Prefers full-screen modals or dedicated screens for keyboard-heavy input forms (e.g., create transaction: tipe/wallet/kategori pickers + nominal/tanggal/catatan) rather than bottom sheets; explicitly states this ("Create Transaksi aku prefer antara layar baru atau kalo modal pun full screen ukurannya"). Confidence: 0.9
- Wants long lists (e.g., transaction list) to use an infinite-scroll FlatList with filter chips at the top, but explicitly warns against UI overcrowding ("hati hati kepenuhan"): when chips exceed ~4, collapse into a single "Filter" button opening a filter sheet, with chips becoming active-filter badges; if content still risks being too dense, fall back to a dedicated screen or modal rather than cramming it into the current surface. Confidence: 0.8
- Wants pickers/selectors to filter options by the selected type or context so data entry stays type-consistent (e.g., category picker in create transaction shows only income categories for income, only expense categories for expense, and no category picker at all for transfer). Confidence: 0.7
- Prefers inline CRUD for reference data directly inside the picker/form where it is used (e.g., create/update/delete categories from within the category picker without leaving the form), and expects the same inline-CRUD behavior to apply consistently in every screen that uses that picker (create transaction and Plan). Confidence: 0.7
- Proposes daily-scoped browsing patterns for transaction lists: a daily view as the default with a date stepper (‹ ›) to step day-by-day, tap-to-jump via a calendar picker, and relative labels ("Hari Ini"/"Kemarin") — i.e., day-focused navigation over a flat endless list, even when an aggregated mode also exists. Confidence: 0.8
- Wants design-agent prompts (Stitch, Figma Make, Open Design) to be polished as thoroughly as possible — encoding locked product decisions, interaction patterns, and quality rules (semantic colors, nominal formatting, empty/loading states) — because prompt quality is what determines design output quality; explicitly asks for this ("titip polish design-agent-prompts lagi sebaik mungkin agar hasil design agentnya bagus dan sesuai"). Confidence: 0.6
- Anchors UI component designs to concrete, proven apps rather than abstract descriptions — e.g., explicitly asked for a category picker "seperti di aplikasi Money Tracker" (grid of round colored tiles with emoji, type tabs, "+" tile, long-press edit) — expects the agent to explain the current adoption and then map the referenced app's pattern onto the project's constraints. Confidence: 0.6
- Reasons from UI/design decisions through to their data-model implications — e.g., adopting an icon-tile category picker immediately means the category table must store an `icon` name — and prefers lightweight references (a name string, not an image asset) so the DB and backup JSON stay simple. Confidence: 0.6
- Prefers bundling static presentation assets (e.g., a curated icon set from `@expo/vector-icons` stored as icon-name strings, searchable/grouped) over runtime API fetches or seeding them into the local DB; accepted the agent's recommendation of bundled icons over his own two alternatives ("hit API master icons" or "store dulu icon sebanyak-banyaknya ke db local") once the redundancy argument was made; reaffirmed the choice with a plain "setuju" when asked to confirm. Confidence: 0.6
- Keeps the first-run onboarding minimal (no account/sign-up) and wants settings that are per-feature to be changed directly inside the feature's own view rather than in onboarding or a global settings screen — e.g., budget period start date editable on the Rencana (Plan) screen, and no wallet "type" field in onboarding; expects all design/spec/prompt artifacts updated consistently with this. Confidence: 0.7
- Before locking the design, asks the agent to audit coverage and flag every screen/view that is still under-specified ("adakah view-view lagi yang masih kurang clear dan perlu dibahas?"), with the agent expected to enumerate the gaps, rank them by discussion priority (e.g., Plan screen first as the core), and then tackle them one at a time; re-confirmed repeatedly by prompting to continue the remaining under-specified screens ("lanjut bahas layar yang belum clear", "lanjut bahas yang belum clear, berikan detailnya", "oke apalagi yang belum clear" — wanting full detail presented per view, with the agent enumerating gaps and ranking them by priority). Confidence: 0.95
- When a prototype/experiment is canceled, expects all generated code to be removed completely so the workspace stays clean — explicitly instructed "cancel prototype plan, hapus juga code yang digenerate"; no throwaway routes/files may be left behind. Confidence: 0.7
- Prefers settling design questions through discussion and docs (the "ngalir" flow) over building throwaway code prototypes; canceled a code prototype of the Plan screen and reverted to paper-level design discussion. Confidence: 0.5
- Wants AI features to be read-only with execution left to the user: AI output is structured, actionable items (each with an explicit "Terapkan"/Apply button that mutates the plan), while the AI itself never writes data — explicitly avoids AI automation; endorsed this "apply per suggestion" model over read-only plain text or interactive chat for MVP. Confidence: 0.7
- Prefers reusing the same form for create and edit states (one form, two modes: pre-filled, retitled, save button relabeled) instead of adding a separate detail screen or bottom sheet to view before editing — chose "langsung ke UI yang sama seperti Create Transaksi akan tetapi edit state" over the agent's recommended detail-bottom-sheet option for the transaction edit flow, on the grounds that it avoids an extra screen/layer. Confidence: 0.8
- Wants screens re-drawn as full ASCII wireframe mockups during design review to recap all locked decisions ("gambarkan lagi screennya") — a complete screen-by-screen visual walkthrough of the app flow rather than text-only summaries. Confidence: 0.5
- Expects UI interaction patterns to be consistent across screens — e.g., asked whether the report period can be customized "the same way as the plan period" (shared Budget period with custom start date, same `[1–30 Sep ▾]` selector pattern), expecting one consistent pattern reused across Plan and Report rather than per-screen divergent behavior; context-specific differences (e.g., report navigates any period/range) are fine, but the underlying pattern stays the same. Proactively spots and flags cross-screen inconsistencies himself and expects them unified rather than justified per-screen — e.g., questioned why the AI trigger button sat in the Plan header but at the bottom of Report ("aku merasa kurang konsisten") and endorsed moving it to the header of both screens. Confidence: 0.7
- Requests iterative whole-plan audits to surface potential issues, gaps, and leftover inconsistencies from superseded decisions — and expects the agent to generate solutions and apply them to every artifact immediately ("hasilkan solusinya dan terapkan") rather than stopping at a findings report; the audit ends only when all docs (CONTEXT/glossary, spec, design brief, prompts) and the synced GitHub issue are consistent. Confidence: 0.8
- Optimizes AI-tool workflows around free-tier credit/limit constraints: being on a free tier, explicitly asks that design-agent prompts be set to direct-build (one-shot, complete output of all screens) instead of plan mode, because plan mode burns free-tier limits ("aku pake free tier jadi tolong buat yang cocok untuk langsung build daripada plan dulu"); expects prompts to explicitly forbid plan mode and to conserve credits by refining per-screen/point-and-edit rather than regenerating everything from scratch. Confidence: 0.9
- Drives requirement/domain audits from concrete real-life usage scenarios rather than abstract principles — surfaces specific edge cases (e.g., "gajian 27, isi 31": salary lands on the 27th but is logged on the 31st; "balance correction vs plan" raised as a distinct concern from planned income) and expects those cases to be worked through as domain-modeling decisions that get recorded in CONTEXT.md/ADR, not hand-waved away. Confidence: 0.6
- States core domain invariants explicitly and expects design to follow from them: wallet balance is the single source of truth ("balance di wallet harus selalu menjadi source of truth") — only transactions may write to the balance, and plan-layer values (e.g., planned income on the Plan screen) are targets or realized figures derived from transactions, never a second write path that could double-count (e.g., logging a salary income transaction and then filling in plan income that auto-creates another transaction). Reasserted when worrying that the plan's computed figures (pendapatan − pengeluaran) could differ from the wallet: every money number in the plan must be explainable from / derived from the same transaction source as the wallet, with only forward targets set manually — alignment must hold by construction, not by manual reconciliation ("plan itu datanya gaboleh beda sama dompet"). Confidence: 0.9
- Wants interview/audit questions handled strictly one at a time: explicitly halts the agent from moving to the next round until the current question is settled ("jangan ke mana mana dulu ya, jawab ini dulu") — do not stack questions or proceed ahead while the user is still formulating a position. Confidence: 0.6
- Wants to be able to correct wallet balances directly in the UI ("Aku mau bisa tinggal koreksi saldo wallet langsung") rather than being forced to reconstruct forgotten/unrecorded spending — prefers a simple direct action whose underlying mechanics (e.g., an auto-created adjustment transaction that keeps plan and wallet aligned without touching planned targets or expense progress) are handled by the app, not by the user. Confidence: 0.7
- Prefers human-error handling to be visible-and-fixable rather than hard-blocked: duplicate income entries are acceptable ("human error tapi bisa notice") because the plan surfaces the duplication (realized vs target), and the fix is deleting the erroneous entry from the plan view — which cascades to delete the underlying transaction and restore the wallet balance. Confidence: 0.5
- Prefers modeling savings/goals as real money movement: contributing to a goal creates an actual transfer transaction from the chosen wallet to a goal wallet (source balance decreases, goal wallet balance increases), so goal progress derives from transactions and plan↔wallet stays aligned by construction — explicitly overrode the previously recorded "goal tracker keeps money in the wallet" model with a worked example. Confidence: 0.6
- Prefers distinguishing entity roles via a boolean flag on a shared primitive rather than introducing a separate entity/type — e.g., a goal wallet should be "a wallet like any other, but with a savings flag" rather than a distinct wallet kind; expects the flag to also unlock adjacent behaviors (emergency withdrawal from goal savings), i.e., values a single design choice that resolves multiple cases at once. Confidence: 0.6
- When writing prompts for design agents (e.g., Figma Make), wants only theme/design-direction guidance (tema, arah desain) — NOT concrete visual specifications such as specific fonts, exact colors, radius, or spacing; specific visual decisions should be delegated to the design agent to decide and keep consistent across screens ("kamu cuma ngarahin tema designnya gimana tapi ga spesifik ngasih tau UInya harus gimana... biar keputusan-keputusan spesifik terkait visual serahkan ke design agentnya"). Reasserted this principle and extended it to ALL design guidance artifacts, not just prompts: objected to concrete hex values and font names in `tokens.css` and in guide examples ("kenapa kita menyarankan secara spesifik visual designnya? kan aku udah bilang boleh arahin nuansa aja, ga sampe spesifik gitu") — docs must state roles/principles with values left empty for the design agent to fill. Confidence: 0.95
- Wants guides for external design tools to be grounded in the tool's actual mechanics verified from primary sources (fetching the tool's repo/docs before advising), because tools differ materially in workflow — e.g., Open Design runs a local coding agent and renders one screen per render (vs Figma Make's all-screens-in-one-shot), so the guide had to be restructured around DESIGN.md as a brand contract + per-screen prompts rather than a one-shot mega-prompt. Re-verifies against primary sources each time before starting ("pastikan sudah paling tepat dan tidak ada yang tertinggal") — the audit re-fetched the actual template seed and official checklist and corrected factual inaccuracies against them. Confidence: 0.85
- Expects the agent to produce the tool-specific artifacts the external tool actually needs (e.g., Open Design's `DESIGN.md` brand contract with ≥7 H2 sections, and a token file) as part of the ultimate guide, and wants those artifacts written into the repo's `docs/design/` so they can be reused for rendering. However, the token artifact must be role/slot-only (`design-tokens.md` with `--bg`, `--success`, `--font-display`, etc. and no values): the earlier concrete-value `tokens.css` (hex colors, font names) was rejected as violating the nuance-direction principle and deleted outright rather than emptied. Confidence: 0.8
- Wants a ready-to-paste starter prompt for the first screen included in the guide (e.g., Beranda with concrete sample data and theme direction), so they can start immediately without composing the prompt themselves. Confidence: 0.7
- Explicitly guards against documentation redundancy: wants each topic owned by exactly one file ("satu sumber per concern") so overlapping design docs do not restate each other — e.g., design-brief points to `DESIGN.md`/`tokens.css` instead of repeating visual principles, and design-agent-prompts drops its Open Design section in favor of a pointer to the dedicated guide. Expects overlapping/duplicated sections to be consolidated into references rather than kept in parallel ("hindari redudancy karena aku takut ada redudancy"). Confidence: 0.8
- When ready to actually start using an external design tool (e.g., Open Design), asks the agent to walk him through the operational steps so he can drive the tool himself — pre-flight setup (install the tool, confirm coding-agent CLI detection, point the working directory at the repo), the exact click-flow (artifact type, design-system/direction choice, model selection), and the ready-to-paste first prompt — rather than a theoretical explanation or the agent running it for him ("pandu aku bagaimana mulai minta open design mulai membuat design"). Confidence: 0.6

